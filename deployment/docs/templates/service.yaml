apiVersion: v1
kind: Service
metadata:
  name: docs-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: docs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: docs