apiVersion: v1
kind: Service
metadata:
  name: fileserver-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: fileserver
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: fileserver