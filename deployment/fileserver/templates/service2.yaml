apiVersion: v1
kind: Service
metadata:
  name: datasets-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: datasets-filesystem 
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: datasets-filesystem 