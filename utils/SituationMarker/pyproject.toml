[project]
name = "gamecontrollistener"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pynput>=1.8.1",
    "vaapi",
]

[tool.uv.sources]
vaapi = { path = "../../sdk" }
